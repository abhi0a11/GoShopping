import { ReactTyped } from "react-typed";

const TypedJs = () => (
  <div>
    <ReactTyped
      strings={["Furniture", "Home Decor", "Electronics", "Export"]}
      typeSpeed={40}
      backSpeed={50}
      loop
    ></ReactTyped>
  </div>
);

export default TypedJs;
